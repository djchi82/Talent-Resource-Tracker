<p id="notice"><%= notice %></p>

<p>
  <b>Name:</b>
  <%= @type.name %>
</p>

<table>
	<thead>
		
		<tr>
			<th>Name</th>
			<th>Description</th>
		</tr>
	</thead>
	<tbody>
	
	<%= render @type.skills %>
	
	</tbody>
</table>

<%= link_to 'Edit', edit_type_path(@type) %> |
<%= link_to 'Back', types_path %> |
<%= link_to "Add Skill", new_skill_path(:type_id => @type.id)  %>
